#!/usr/bin/env bash
set -euo pipefail

export RUN_MODE="${RUN_MODE:-eval_only}"
export ARTIFACT_PATH="${ARTIFACT_PATH:-final_model.int6.ptz}"
export VAL_MAX_TOKENS="${VAL_MAX_TOKENS:-65536}"
export MLP_LEAKY_SLOPE="${MLP_LEAKY_SLOPE:-0.75}"
export COMPLEMENT_ALPHA="${COMPLEMENT_ALPHA:-0.5}"
export COMPLEMENT_STATS_MAX_TOKENS="${COMPLEMENT_STATS_MAX_TOKENS:-1000000}"
export QUANT_BITS="${QUANT_BITS:-6}"
export NGRAM_ENABLED="${NGRAM_ENABLED:-1}"
export NGRAM_MIN_ORDER="${NGRAM_MIN_ORDER:-2}"
export NGRAM_MAX_ORDER="${NGRAM_MAX_ORDER:-6}"
export NGRAM_NUM_BUCKETS="${NGRAM_NUM_BUCKETS:-250000}"
export NGRAM_ALPHA_BASE="${NGRAM_ALPHA_BASE:-0.05}"
export NGRAM_ALPHA_RANGE="${NGRAM_ALPHA_RANGE:-0.55}"
export NGRAM_ENTROPY_CENTER="${NGRAM_ENTROPY_CENTER:-4.0}"
export NGRAM_ENTROPY_SCALE="${NGRAM_ENTROPY_SCALE:-2.0}"
export NGRAM_EVAL_TOKENS="${NGRAM_EVAL_TOKENS:-65536}"
export TTT_ENABLED="${TTT_ENABLED:-1}"
export TTT_CHUNK_TOKENS="${TTT_CHUNK_TOKENS:-32768}"
export TTT_LORA_RANK="${TTT_LORA_RANK:-4}"
export TTT_LORA_LR="${TTT_LORA_LR:-0.002}"
export TTT_EPOCHS="${TTT_EPOCHS:-3}"
export TTT_EVERY_N_CHUNKS="${TTT_EVERY_N_CHUNKS:-1}"

python train_gpt.py
